// Imports
import React from "react";
import classnames from "classnames";
import PropTypes from "prop-types";

// Creating the select field group
// ---------------------------------------------------------------------------------------

const SelectFieldGroup = ({ id, value, className, label, errors, type, onChange, placeholder, name, checkUser, field }) => {


}